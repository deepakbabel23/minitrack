# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Linux
*~
.directory
.Trash-*
.fuse_hidden*
.nfs*

# SQLite database (created on first run)
minitrack.db

# Local environment — holds MINITRACK_API_KEYS. Never commit.
# .env.example IS tracked and is the template to copy.
.env
.env.*
!.env.example

# Node / frontend
node_modules/
dist/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Test / tooling caches
.pytest_cache/
.coverage

# Claude Code — personal/machine-local settings (shared settings.json IS tracked)
.claude/settings.local.json
