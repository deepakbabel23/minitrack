# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# MS Office lock files, left behind while a doc is open (e.g. ~$deck.pptx)
~$*

# Linux
*~
.directory
.Trash-*
.fuse_hidden*
.nfs*

# SQLite database (created on first run)
minitrack.db

# Local environment — holds MINITRACK_API_KEYS. Never commit.
# .env.example IS tracked and is the template to copy.
.env
.env.*
!.env.example

# Node / frontend
node_modules/
dist/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Test / tooling caches
.pytest_cache/
.coverage

# Claude Code — personal/machine-local settings (shared settings.json IS tracked)
.claude/settings.local.json

# Superseded diagram set — replaced by minitrack_frontend_diagrams_updated/,
# which ships the same eight diagrams as editable SVG alongside the PNGs.
# Kept on disk, out of history.
facilitator/minitrack_frontend_diagrams/
